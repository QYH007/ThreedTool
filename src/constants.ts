import { config } from '@react-spring/three';

export const DEBUG_MODE = false;

export const FAST_ANIMATION_SPEED_IN_MS = 150;
export const DEFAULT_ANIMATION_SPEED_CONFIG = config.default;

export const FACE_NORMALS_COLOR = 0xff0000;
export const VERTEX_NORMALS_COLOR = 0x12ec5b;

export const ILLUMINATION_LIGHT_HELPER_COLOR = '#03fc35';
