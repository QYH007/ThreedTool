import { Divider } from '@material-ui/core';
import React, { ReactElement } from 'react';

export const StyledDivider = (): ReactElement => <Divider light variant="middle" style={{ marginBottom: '10px' }} />;
